<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#2196F3">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="top|center_horizontal"
        android:orientation="vertical"
        android:paddingTop="24dp"
        android:padding="24dp">

        <!-- 🔵 Header Section -->
        <TextView
            android:id="@+id/txtWelcome"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Welcome to Freetime Wallet"
            android:textStyle="bold"
            android:background="#60afefff"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:gravity="center" />

        <TextView
            android:id="@+id/txtLoginOrGen"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Login Using Your existing Passphrase or Generate one"
            android:textColor="#FFFFFF"
            android:background="#964300ff"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginTop="16dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnGen"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Generate Passphrase"
            android:layout_marginTop="32dp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#4CAF50" />

        <EditText
            android:id="@+id/inputPassphrase"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:hint="Enter your passphrase here"
            android:inputType="textMultiLine"
            android:minLines="3"
            android:maxLines="5"
            android:padding="12dp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#4CAF50"
            android:textSize="16sp"
            android:background="@android:drawable/edit_text" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnLogin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Login Using Your Passphrase"
            android:layout_marginTop="32dp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#4CAF50" />

        <!-- 🔴 Graph/Data Visualization -->
        <TextView
            android:id="@+id/priceDisplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Price will appear here"
            android:textSize="16sp"
            android:textColor="#444"
            android:padding="12dp"
            android:layout_marginTop="16dp"
            android:background="#ff0000ff" />


        <!-- 🪙 Coin Selector -->
        <TextView
            android:id="@+id/txtSelectCoin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Select a coin to view its address:"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:layout_marginTop="32dp" />

        <Spinner
            android:id="@+id/coinSelector"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:entries="@array/coin_array"
            android:enabled="false" />

        <!-- 📬 Address Display -->
        <TextView
            android:id="@+id/addressDisplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Address will appear here"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:padding="12dp"
            android:layout_marginTop="16dp"
            android:background="#E0E0E0" />

    </LinearLayout>
</ScrollView>
